# Build output. Everything here is reproducible with `make`.
build/

# Live stores and transfer bundles must never be committed: they contain
# derived descriptions of a user's files, which §27.3 warns can be more
# sensitive than the originals.
*.chutni/
*.chutnipack

# Editor and OS noise.
.DS_Store
*.swp
compile_commands.json

# Python test byproducts.
__pycache__/
*.pyc
